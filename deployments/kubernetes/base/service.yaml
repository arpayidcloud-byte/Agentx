apiVersion: v1
kind: Service
metadata:
  name: agentx-api
  labels:
    app: agentx
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: agentx
    component: api
